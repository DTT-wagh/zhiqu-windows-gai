# 接口契约目录

接口契约先于实现提交。每个契约至少写明：方法和路径、鉴权、请求字段及示例、成功响应及示例、错误响应、幂等性、前端展示方式和兼容要求。

- `TEMPLATE.md`：新功能复制模板。
- `community-answer-images.md`：回答图片的当前契约示例。

接口字段需要变更时，先增加兼容字段或更新契约版本，再同时修改服务端和调用方。不能只修改后端或只修改前端。
