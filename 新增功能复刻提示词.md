# 智趣 AI 学堂新增功能复刻提示词

这份文档只保留后续新增或改造的用户功能，不包含项目启动、端口调整、密钥配置、视频配置、故障排查、文档和 Git 操作。

使用方式：将每一个“提示词”代码块原样交给开发者或编程助手执行。要求对方先阅读现有代码并沿用项目已有的 UI、路由、接口和数据结构；不要重构无关功能，也不要把真实密钥写入源码或提交到仓库。

## 1. 社区新增第二组 AI 实践游戏

```text
在社区的“AI 实践”游戏页新增第二组“四款 AI 实践游戏”。

位置：第一组“四款 AI 实践游戏”下方、“公开房间”上方。

要求：
1. 第二组必须正好有 4 张游戏卡片，内容暂定为“新游戏一、二、三、四”。
2. 每张卡都展示“内容筹备中 / 玩法内容待定，新的 AI 实践挑战正在设计中。/ 通过观察、提问和验证，在游戏中练习使用 AI。/ 敬请期待”。
3. 卡片不可进入游戏，点击不跳转；要具备禁用状态和无障碍说明。
4. 第二组必须直接复用第一组游戏的同一个区块结构、卡片结构、网格列数、尺寸、间距、字体、图标区域和响应式规则，不能另做一套大卡片或不同布局。
5. 在桌面端和移动端均应与第一组视觉一致；第二组不能只显示一张或三张卡片。
6. 从“帖子”等社区子栏目切换回“AI 实践”、从个人页保存资料后再回到社区、浏览器前进后退或页面重新渲染时，第二组都必须稳定存在，且不能重复插入。
7. 原有第一组四款可玩的 AI 实践游戏、公开房间和其他社区功能不得改变。

完成后用浏览器验证上述切换路径，并截图确认两组卡片大小和排布完全一致。
```

## 2. “我的”页面双击头像查看大图

```text
在“我的”个人主页增加头像大图预览，交互参考主流社交 App。

要求：
1. 仅在“我的”主页生效。桌面端双击头像、触摸设备双击/双点头像时打开预览；单击头像不触发，避免影响页面原有操作。
2. 打开全屏深色遮罩层，头像按原图比例居中显示，最大宽高不超过可视区域；不得拉伸或裁切头像主体。
3. 预览页顶部有清晰的关闭按钮；点击遮罩空白区域、点击关闭按钮或按 Escape 都可退出。
4. 打开时锁定页面背景滚动，关闭时恢复原来的滚动状态和焦点。
5. 预览应有平滑、克制的出现和关闭动效，并遵从系统“减少动态效果”设置。
6. 图片加载失败时不能导致页面报错或留下无法关闭的遮罩。
7. 不改变头像上传、编辑资料或其他个人页逻辑。

完成后分别验证鼠标双击、移动端双点、遮罩关闭、Escape 关闭和背景不可滚动。
```

## 3. 编辑资料的生日滚轮选择器

```text
将“编辑资料”页面的生日输入改为移动 App 常见的底部滚轮选择器，年、月、日必须分为三个独立滚轮。

要求：
1. 点击生日字段时不直接弹出键盘，改为从底部弹出的选择面板；面板有“取消”“选择生日”“完成”。
2. 三列分别为年、月、日，中央有明确选中框；滚动停止后自动吸附到完整的一项。
3. 年份范围为当前年份向前 100 年至当前年份；月份为 1-12；日期根据年月实时变化，正确处理闰年和每月天数。
4. 初始值读取用户已保存生日；无效或空值使用合理默认日期。最终写回格式必须是 YYYY-MM-DD。
5. 点击“取消”或遮罩空白处不修改资料；点击“完成”才把当前选择同步到页面表单，并触发现有表单状态更新。
6. 支持键盘上下箭头操作，控件具有明确的“年份、月份、日期”无障碍标签；按 Escape 可关闭。
7. 桌面和移动端均有合理尺寸、不会文字溢出，也不遮挡保存按钮。
8. 保留并修正资料保存体验：点击“保存资料”显示保存中反馈；成功显示“资料保存成功”并返回个人页；失败显示接口错误或网络错误；返回箭头必须能回到个人页。

完成后验证普通日期、2 月 29 日、切换月份导致日期收缩、取消、完成、保存成功和保存失败的反馈。
```

## 4. 发布问题页面的主题、正文和导航交互

```text
优化社区“提问题”页面，交互参考成熟内容社区的发帖页。

要求：
1. “选择主题”支持多选；已经选中的主题再次点击即可取消选中，取消后主题不应出现在提交数据中。
2. 问题正文最大长度改为 3000 字/字符，并让前端限制、字数提示、后端校验和数据库字段保持一致，不能只改其中一层。
3. 正文输入框适合长内容编辑：初始至少可舒适查看约 10 行，输入时随内容自动增高；到合理最大高度后在框内滚动，不能让内容只露出约 5 行。
4. 顶部提供明确的返回箭头和“取消”操作。两者均返回社区列表，不提交当前草稿；按钮在移动端可触达且不与标题重叠。
5. 提交按钮的禁用、校验失败、提交中、成功和失败状态应有明确反馈。所有必填项满足后必须可以正常提交。
6. 不改变已有问题列表、问题详情和编辑问题页面的行为；编辑问题页面如复用正文输入框，也应获得同样的长文本体验。

完成后验证：主题选择和取消、3000 字边界、长正文编辑、返回/取消、成功提交和校验失败。
```

## 5. 问题详情页的回答输入框滚动规则

```text
改造问题详情页的“参与讨论，写下你的回答…”输入框，交互参考百度贴吧等内容社区。

要求：
1. 初始状态下，收起的回答输入框位于“回答 N”标题下方、第一条回答上方，属于回答区内容流的一部分。
2. 用户滚动页面时，输入框应随回答区一起移动；只有当回答区顶部已经被屏幕上边缘盖过、原位置不能继续看见时，输入框才固定到屏幕底部。
3. 当输入框仍在回答区顶部的内容流中时，点击它必须在原地展开为编辑器，不能跳到屏幕底部。
4. 当输入框已经固定在底部时，点击它就在底部展开编辑器。
5. 展开后有“收起”操作和 Escape 关闭；收起后恢复与当前滚动位置相匹配的“内容流/底部固定”状态。
6. 无论输入框在内容流还是底部固定，均不能遮挡第一条回答、其他用户回答或页面末尾内容。为固定状态和展开状态预留等高的滚动底部空间。
7. 视口改变、回答数变化、路由切换、浏览器前进后退时都要重新计算位置，不得出现闪跳、重复输入框或遮挡。
8. 问题详情页顶部要有明确返回社区的按钮；直接进入页面时也能返回社区列表。

完成后在桌面和移动宽度下验证：未滚动、滚动到触发点前、滚动到触发点后、原地展开、底部展开、收起、返回页面。
```

## 6. 回答中添加图片

```text
为问题详情页的“回答”编辑器增加图片附件能力，体验参考主流社区回复功能。

要求：
1. 在展开的回答编辑器中提供“添加图片”按钮，可选 JPG、JPEG、PNG、GIF、WebP；支持一次多选，最多 9 张，单张最大 5MB。
2. 选中图片后立刻显示等比例缩略图网格和已选数量；每张图片可删除；点击缩略图可预览大图。
3. 点击提交回答时先上传图片，上传中禁用重复提交并显示清晰状态；任一张上传失败时给出错误反馈且不丢失用户已输入的文字和已选图片。
4. 回答发布成功后，图片与文字一起显示在该回答下方；1 张图使用单列合适宽度，多图使用紧凑网格；点击已发布图片可全屏查看，点击遮罩、关闭按钮或 Escape 可关闭。
5. 已发布图片不能依赖浏览器自动携带登录令牌：后端图片读取接口应允许匿名 GET，上传接口仍必须要求已登录用户。
6. 后端只接受上述图片 MIME 类型和 5MB 以内文件；使用随机文件名，阻止路径穿越；文件保存到专用回答图片目录；读取时设置正确 Content-Type、nosniff 和长期缓存头。
7. 返回的图片 URL 必须能被普通 img 标签直接加载。不要把真实图片、用户的本地文件路径或密钥写入源码。
8. 对旧回答中的图片标记或已有图片数据兼容显示；页面重新加载、从其他页面返回以及切换账号后，已发布图片仍可正常显示。

完成后用真实图片验证：上传、删除、9 张限制、超 5MB 拒绝、格式拒绝、提交、刷新后显示、匿名图片 GET 为 200 且 Content-Type 正确、全屏预览和关闭。
```

## 7. 一次性实施全部新增功能时的总提示词

```text
请在现有“智趣 AI 学堂”项目中实现以下已确定的新增功能，并逐项验收：

1. 社区 AI 实践页：在原四款可玩游戏和公开房间之间，增加第二组同尺寸、同布局的四张“内容筹备中”占位游戏卡。
2. 我的主页：双击头像打开全屏大图预览，可通过遮罩、关闭按钮和 Escape 退出。
3. 编辑资料：生日改为年、月、日三列底部滚轮；保存资料显示加载、成功或失败反馈，成功回个人页，返回箭头可靠可用。
4. 提问题：主题可再次点击取消；正文上限 3000 且适合长文本编辑；顶部有返回和取消；可正常提交并反馈状态。
5. 问题详情回答区：输入框初始在回答标题下随内容滚动，只有被屏幕顶部越过后才固定在底部；在内容流中点击必须原地展开，固定后才在底部展开；不得遮挡回答。
6. 回答编辑器：支持最多 9 张、单张 5MB 的 JPG/JPEG/PNG/GIF/WebP 图片，带缩略图、删除、上传状态、发布后网格展示和全屏预览；图片上传鉴权，图片读取允许普通 img 匿名访问。
7. 问题详情页必须有明确返回社区的操作。

通用约束：
- 先阅读现有项目代码、路由、样式和接口，沿用现有设计语言与数据结构。
- 不改变现有已实现功能，不删除原有四款可玩游戏，不引入无关依赖或大范围重构。
- 所有页面在桌面与移动宽度均无元素遮挡、文字溢出或重复挂载。
- 任何涉及真实 API 密钥、用户资料、数据库、上传文件的内容都不能提交到源码仓库。
- 每项完成功能后都要通过浏览器做真实交互验证，而不仅是静态代码检查；最后列出修改文件与验收结果。
```
